function solution(A, K) {
  //Reading the Problem set.

  //Trying to get the length
  let length = A.length;

  newPosition = length-3%length
  
  console.log(length);
}

const A = [1, 2, 3, 4, 5, 6];
solution(A, 2);
